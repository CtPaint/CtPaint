_         = require 'lodash'
app       = Elm.Main.fullscreen()
# {fromJS} = app.ports

# fromJS = (thing) ->
#   app.ports.fromJS.send thing


# app.ports.toJS.subscribe (thing) ->
#   'this is a ' + thing

